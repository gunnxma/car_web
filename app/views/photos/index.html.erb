<%= form_for @photo, url: :photos, :html => { :class => "form-horizontal" } do |f| %>
<%= f.hidden_field :car_info_id %>
<%= f.hidden_field :photo_style %>
<%= f.file_field :photo %>
<%= f.submit "上传" %>
<% end %>

<ul class="thumbnails gallery">
<% @photos.each do |photo| %>
  <li id="image-1" class="thumbnail">
    <a style="background:url(<%= photo.photo.url(:thumb) %>)" href="<%= photo.photo.url %>"><img class="grayscale" src="<%= photo.photo.url(:thumb) %>"></a>
							</li>
<% end %>